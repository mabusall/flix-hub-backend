version: '3.8'

services:
  flixhub-api:
    build:
      context: .
      dockerfile: Application/Services/FlixHub.Api/Dockerfile
    container_name: flixhub-api
    ports:
      - "2535:80"
      - "6979:443"
    env_file:
      - .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
    networks:
      - flixhub-network

networks:
  flixhub-network:
    driver: bridge
